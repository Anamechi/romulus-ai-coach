# Update "Work With Me" CTA destination

Change the "Work With Me" button link from `/contact` to `/revenue-architecture-session` in the two places it appears on the homepage.

## Files to change

1. `src/components/home/editorial/EditorialHero.tsx` (line 64) — hero CTA
2. `src/components/home/editorial/ClosingCTA.tsx` (line 32) — closing CTA

## Change

Replace `href="/contact"` with `href="/revenue-architecture-session"` on the "Work With Me" anchor in both files. No other copy or styling changes.
